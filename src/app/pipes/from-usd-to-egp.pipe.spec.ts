import { FromUsdToEgpPipe } from './from-usd-to-egp.pipe';

describe('FromUsdToEgpPipe', () => {
  it('create an instance', () => {
    const pipe = new FromUsdToEgpPipe();
    expect(pipe).toBeTruthy();
  });
});
